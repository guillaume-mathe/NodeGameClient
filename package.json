{
  "name": "node-game-client",
  "description": "Browser game client networking library for node-game-server",
  "author": {
    "email": "guillaume.mathe@gmail.com",
    "name": "Guillaume MATHE"
  },
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/node-game-client.js",
      "default": "./dist/node-game-client.js"
    },
    "./src/*": "./src/*"
  },
  "module": "./dist/node-game-client.js",
  "browser": "./dist/node-game-client.js",
  "types": "./dist/types/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist/",
    "src/"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build:bundle": "node build.js",
    "build:types": "tsc",
    "build": "node build.js && tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "peerDependencies": {
    "node-game-ecs": ">=0.0.1",
    "node-game-input-manager": ">=0.0.1"
  },
  "peerDependenciesMeta": {
    "node-game-ecs": {
      "optional": true
    },
    "node-game-input-manager": {
      "optional": true
    }
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
